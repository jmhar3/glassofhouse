fetch = async () => {
    const respone = await fetch('url')
    const data = await response.json() 
    return data.results}

    const {variable1, variable2} = this.variable
    // multiple variables, same use